spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    #    username: postgres
    username: user_HEtrkZ
    #    password: password_dev
    password: password_xya85y
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    #    show-sql:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health,info,metrics  # 开放健康检查和指标监控

custom:
  welcome-message: "Hello from User Service[native]!"

server:
  port: 1082